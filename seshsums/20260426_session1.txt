Session Summary — 2026-04-26 (Session 1)
=========================================

Project: nanoalgebra
  Tiny GPT-style transformers trained on symbolic algebra to study "understanding,"
  reification, and bounded rationality in small models.

--- What existed at session start ---

  Repo initialized with three commits:
    1. Initial commit (README stub)
    2. Imported Original Spec Discussions — three PDFs/text files in
       20260425_Original_Spec_Discussions/:
         - Jeff_Spec_1.pdf
         - Daivd_Spec_2.pdf
         - Rousell_Spec_3.txt  (full conversation log; serves as master project outline)
    3. First try at training data generator — data_gen.py

  data_gen.py status: complete and working.
    Grammar:  Expr := digit | ('neg',Expr) | ('add',Expr,Expr) | ('sub',Expr,Expr)
    Task:     Distribute all negations to leaves.
              -(a+b) -> -a-b,  -(a-b) -> -a+b,  -(-(a)) -> a
    Format:   "input->target"  (e.g., "-(3+4)->-3-4")
    Vocab:    ()+-0123456789>  (15 chars; '>' only in '->' separator)
    Splits:   train, test_same (same depth), test_deeper (OOD depth generalization)
    Dataset already generated: data/20260426_135510/

--- What happened this session ---

  1. Oriented to project state by reading data_gen.py and the Rousell spec log.
  2. Created seshsums/ directory for session summaries (this file).

--- Completed this session ---

  Wrote train.py: minimal decoder-only transformer (nanoGPT style) in PyTorch.
    - GPTConfig dataclass; NanoGPT (tok_emb + pos_emb + N x Block + LN + head, weight-tied)
    - CausalSelfAttention with registered causal mask buffer
    - AlgebraDataset: pads to max_len, builds loss mask covering only answer tokens (after '>')
    - Loss: cross-entropy masked to answer side only
    - Eval: exact-match accuracy (all answer positions must be correct)
    - greedy_decode: extends a prompt token-by-token until PAD or max_len
    - LR schedule: linear warmup + cosine decay
    - Prints sample predictions (greedy) at end of training
    - CLI via argparse with sensible defaults (2L 4H d128 max_len=256 5000 iters)
    - Sanity-checked: 430K params, forward pass OK, eval loop OK, greedy_decode OK
    - Python to use: /usr/local/bin/python3.10 (has torch 1.13.1)

--- First training run results (2L 4H d128, 5000 steps, ~16 min CPU) ---

  test_same  (depth-2, in-distribution):  1.000  (100% exact match)
  test_deeper (depth-4, OOD):             0.173  (17.3% exact match)

  Convergence: loss hit ~0.02 by step 400, plateaued near 0.0006 by step 2000.
  test_same reached 1.000 at step 1000 and stayed there.
  test_deeper stalled at ~17% throughout — the key generalization gap.

  Notes:
  - max_len reduced from 256 to 96 (actual data max is 84 for depth-4);
    gave ~8x speedup in attention (O(T^2)).
  - Greedy decode display is cosmetically broken (no EOS token; model
    keeps generating past the answer). Accuracy metric is unaffected.
    Fix: add EOS token (future work) or cap generation in visualization.
  - Save crashed (missing models/ dir); re-run in progress.
  - Python to use: /usr/local/bin/python3.10 (torch 1.13.1, no MPS).

--- Next steps ---

  1. Fix greedy decode (EOS token or length cap in display)
  2. Curriculum experiment: train on depth-1 → depth-2 → depth-4 progressively;
     does it improve test_deeper generalization beyond 17%?
  3. Probes: attention pattern visualization, activation patching
  4. Misconception induction: biased training data

--- Key project context (from spec discussions) ---

  Research questions:
    - Reification: do implicit procedures become explicit reusable concepts?
    - Misconceptions: procedural errors vs. coherent wrong internal "understandings"
    - Algebraic reasoning emergence: general principles over classes
    - Bounded rationality (Simon/Gigerenzer): compact abstractions that generalize

  Training regimes (central experimental axis):
    - Joint/Random: all expression types mixed
    - Progressive/Curriculum: digits -> add/sub -> neg -> fractions/division

  Generalization probes:
    - Held-out digits (model has embeddings but never sees them in training)
    - Novel recombinations of learned sub-expressions (compositional generalization)
    - Deeper nesting (OOD depth)

  Theoretical framing:
    - Reification (Sfard, Dubinsky APOS, Tall procept)
    - Dehaene number sense (implicit magnitude foundations)
    - Simon scissors + Gigerenzer ecological rationality
